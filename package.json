{
  "name": "whatsapp-unsubscribe",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/_worker.js --format=esm --target=es2022",
    "dev": "wrangler pages dev dist",
    "deploy": "wrangler pages deploy dist"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
